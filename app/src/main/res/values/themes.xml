<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ReminderScreen" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="elevationOverlayEnabled">false</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="alertDialogTheme">@style/CustomAlertDialog</item>

        <!-- Popup Menu Styling -->
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="popupMenuStyle">@style/PopupMenu</item>
        <item name="android:textAppearanceLargePopupMenu">@style/PopupMenuText</item>
        <item name="android:textAppearanceSmallPopupMenu">@style/PopupMenuText</item>
    </style>

    <style name="Theme.ReminderScreen" parent="Base.Theme.ReminderScreen" />

    <style name="PopupMenu" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@drawable/popup_background_white</item>
        <item name="android:dropDownVerticalOffset">4dp</item>
    </style>

    <style name="PopupMenuText" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">12dp</item>
    </style>

</resources>